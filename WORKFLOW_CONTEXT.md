# 厌氧型封膜机当前工作流上下文

> 后续修改本项目的运动、吸膜、释放、封膜或流程节拍前，先阅读本文档，再阅读相关源文件和最新现场日志。
> 
> 本文记录的是截至 2026-08-05 已现场验证的状态，不替代现场安全确认。

## 设备与流程

- MCU: STM32F405 + FreeRTOS。
- 485 从站地址:
  - `0x01`: 电动吸盘。
  - `0x02`: 吸膜电缸。
  - `0x03`: 封膜电缸。
  - `0x04`: 富俊丝杆电机。
- 主流程顺序:
  1. 吸膜 (`0x0061`)
  2. 铺膜/放膜 (`0x0062`)
  3. 封膜 (`0x0063`)
  4. 取放孔板 (`0x0064`)

主流程和动作实现位于 `Core/Src/common/modbus_slave_reg.c`。

## 当前已确认的运动逻辑

### 富俊丝杆电机

- 当前运行速度: `75000` (`0.01 rpm` 单位，即 `750 rpm`)。
- 定义位置: `Core/Src/app/dev_fujun_motor.c` 中的 `FUJUN_RUN_SPEED_001RPM`。
- 不要恢复成基于 `RUNNING` 状态位的固定启动超时判断。驱动器的 `RUNNING` 状态可能滞后于实际运动，会导致电机已经运动时被误判失败并重复下发运动命令。
- 当前 `fujun_motor_wait_position()` 使用实际位置反馈判断:
  - 到目标位置 `+/-40` 脉冲即成功。
  - 每 `100 ms` 读取位置。
  - 位置仍在变化时持续等待，不能重复发送 `Correction move`。
  - 已经移动过且连续 3 次位置无变化、仍未到目标，才允许补偿。
  - `10 s` 仅为整体安全超时，不是正常到位的固定等待时间。
- 现场现象: 速度 `100000` 会因惯性带动已放下的膜；`75000` 为当前折中值。

### 吸膜电缸

- 未工作时在 `0` 点。
- 吸膜下行目标 `5200` 是最大安全行程，不应把“必须到 5200”当作吸到膜的条件。
- 下行完成的有效判定:
  - `ARRIVED`: 到达最大行程，允许继续吸膜。
  - `STALLED`: 低推力接触膜，允许继续吸膜。
  - 通讯异常或整体超时: 失败，并执行吸盘释放与电缸归零清理。
- 当前接触参数由初始化每次上电写入，不保存到电缸 EEPROM:
  - 正向推压。
  - 推力 `15%`。
  - 推压段 `100` (`1.00 mm`)。
  - 推压速度 `10%`。
- 放膜时吸膜电缸下行到固定点 `1200`，已禁用该段推压长度，避免触底后下压/反弹。

### 封膜电缸

- 下行目标: `5000`。
- 到位由电缸实际位置反馈确认，不按固定 1 秒判断。
- 当前封膜保压时间来自 EEPROM 参数 `0x0079`，单位秒；现场当前值为 `3 s`。

## 吸盘确认与释放

实现文件: `Core/Src/app/dev_suction_cup.c`。

### 吸膜确认

- 吸盘硬件吸取判断时间: `1 s`，初始化写入寄存器 `0x0091`。
- 程序每 `50 ms` 查询吸盘状态。
- 状态 `1` 表示吸住物体。
- 状态 `2` (未吸住) 或 `3` (掉落) 会立即判失败。
- 读到状态 `1` 后，软件稳定等待 `200 ms`。
- 工作流再等待 `150 ms`，然后吸膜电缸回零。
- 因此不要误认为吸膜确认只有 1 秒；实际还包含软件稳定缓冲与轮询开销。

### 放膜释放

- 释放命令写入吸盘寄存器 `0x0002`。
- 状态 `0` 表示释放/初始化。
- 读到释放状态后，`SUC_RELEASE_STABLE_MS = 0`，没有额外 `1000 ms` 纯等待。
- 工作流仍保留 `150 ms` 机械缓冲，之后吸膜电缸回零。
- 当前释放流程:
  `释放命令 -> 读到释放状态 -> 等 150 ms -> 吸膜电缸回零`。
- 膜曾出现释放后两侧翘起。其可能与四角吸嘴释放不同步、残余真空、膜预张力/静电、吸盘抬升过快有关；不要仅靠继续增加释放等待解决。

## 当前流程时间与日志

### 计时日志

- `cmd_start_workflow()` 已记录总耗时及四个复合动作耗时。
- 日志格式示例:
  - `[WORKFLOW] Step1 Suction elapsed: ... result=SUCCESS`
  - `[WORKFLOW] Total elapsed: ... result=SUCCESS`
- 步骤间 `300 ms` 间隔只计入总耗时，不计入四个步骤的耗时。

### 已记录的节拍

| 阶段 | 总耗时 | 说明 |
| --- | ---: | --- |
| 早期计时版本 | `18.051 s` | 速度较低，且富俊启动误判会重复下发移动命令，释放后还有 `1000 ms` 等待。 |
| 修复富俊重复命令后 | `15.640 s` | 富俊速度已为 `75000`，但释放后仍保留 `1000 ms`。 |
| 当前版本 | `14.839 s` | 已删除释放后 `1000 ms` 等待。 |

- 相对第一份可计时日志，当前缩短 `3.212 s`，约 `17.8%`。
- 该差值同时包含富俊速度提升、消除重复命令和删除释放延时，不能从现有日志精确拆分各自的独立收益。

### 当前稳定轮次的分步耗时

| 步骤 | 耗时 |
| --- | ---: |
| 吸膜 | `4.049 s` |
| 铺膜 | `2.479 s` |
| 封膜 | `6.693 s` |
| 孔板 | `0.598 s` |
| 总流程 | `14.839 s` |

## 已知问题与注意事项

1. `slave=0x01` 和 `slave=0x04` 偶尔出现 Modbus 超时，但第一轮重试通常恢复。它会造成节拍波动，应优先检查 RS485 接线、终端电阻、屏蔽接地和从机供电；不要贸然缩短通讯超时。
2. 温度日志中的 `T2=-999.0C` 是无效值或通讯异常标志；`T2=280.3C` 也可疑。当前不会中断主流程，但需要单独检查第二路温度传感器及其读取校验。
3. 当前每两个复合步骤之间固定等待 `300 ms`，共约 `0.9 s`。这是下一项相对低风险的节拍优化候选；建议先现场测试改为 `150 ms`，确认无机械余振、无膜材偏移后再决定是否继续缩短。
4. 封膜 `3 s` 保压是当前最大单项固定等待。若要缩短，必须以封口强度、密封性和膜面质量测试为准。
5. 不要直接把富俊速度恢复到 `100000`，此前现场已验证惯性会带动放下的膜。若需要进一步提高丝杆节拍，优先评估驱动器的加减速参数，而不是只提高最高速度。
6. “电缸回零”和下一段富俊移动理论上可并行，但尚未做机械干涉验证，不能直接并行化。

## 修改后的最低验证要求

- 使用 Keil MDK 对完整工程构建并下载测试。
- 至少执行 3 轮完整工作流，检查:
  - 每个富俊移动段只有一次初始 `Correction move`；不得重新出现 `Run start timeout`。
  - 吸膜电缸在接触膜或到达 `5200` 后才开始吸取。
  - 放膜后膜不会被电缸回升带起。
  - 任意失败或停止时，吸盘释放、吸膜电缸归零、封膜电缸归零、富俊归零均正常。
  - 记录每轮四步骤和总耗时，区分稳定收益与通讯重试带来的偶然波动。
